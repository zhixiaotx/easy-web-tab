# 学生工作台 功能详述（Functional Spec）

> 配套文档：[PRD-学生工作台.md](./PRD-学生工作台.md)
> 版本：v1.0 | 日期：2026-08-30
> 范围：14 个功能模块的 5 维度详细描述 + 原型线框图（精确按钮位置）

---

## 一、功能概述表（Feature Overview）

| # | 模块 | 功能描述 |
|---|------|----------|
| 1 | 学生首页 StudentHome | 问候条 + 三屏轮播（行动台/数据概览/工具栏）+ 学段徽标；6 秒自动轮播 hover 暂停，箭头/圆点切换；行动台卡片点击直达对应面板 |
| 2 | 习惯打卡 StudentHabits | 左表单右卡片网格布局；新增/编辑/删除习惯；日历热力图；连续天数进度环；分类筛选 tabs；学段默认习惯播种 |
| 3 | 作业管理 StudentHomework | 学科筛选 tabs + 状态分组；卡片网格 5 列；新增/编辑/删除/状态切换；到期提醒；一屏分页（5 列 × N 行） |
| 4 | 课程表 StudentTimetable | 周视图表格（行=节次，列=周一到周日）+ 日视图（今日高亮）；批量复制；弹框编辑单节课 |
| 5 | 学习计划 StudentPlan | 周计划/月计划/学期计划列表；目标进度条；复盘编辑；卡片网格 5 列 |
| 6 | 复习计划 StudentReview | 艾宾浩斯算法驱动今日待复习列表；按学科分组；复习完成打卡自动推进阶段；已掌握标记；复习日历 |
| 7 | 错题本 StudentMistakes | 学科+标签双筛选；Markdown 题干编辑；拍照上传（IDB Blob）；复习状态追踪；卡片网格 5 列 |
| 8 | 阅读记录 StudentReading | 阅读列表 + 累计页数/时长统计 + 折线图；新增/编辑；家长签字（小学低段）；卡片网格 5 列 |
| 9 | 考试倒计时 StudentExam | 基于 countdowns 的 exam 分类；学科/范围/目标分数字段；学段默认考试播种（期中/期末/月考/中考） |
| 10 | 学习日记 StudentDiary | 每日一篇 Markdown；学段模板（K 涂鸦/P 拼音/J 完整）；历史卡片分页；date 本地唯一 |
| 11 | 番茄钟 StudentPomodoro | 学段默认时长（小学 25+5 / 初中 50+10）；学科+作业/复习关联；今日累计统计；连续专注徽章 |
| 12 | 成就勋章 StudentAchievements | 勋章墙网格；已解锁高亮，未解锁灰色+进度条；分类筛选 tabs；解锁动画 |
| 13 | 奖励积分 StudentRewards | 积分账户余额；可兑换奖励列表；兑换按钮（扣分）；交易历史；家长手动加分 |
| 14 | 家长协同 StudentParent | PIN 解锁入口；设置每日任务；查看孩子报告；手动加分/发勋章；阅读签字；配置奖励项 |

---

## 二、模块详情（Module Detail）

> 每个模块按以下 5 维度展开：业务逻辑 / 交互逻辑 / 规则约束 / 权限逻辑 / 边界异常
> 原型线框图采用 ASCII 线框，按钮位置以「顶部左/右」「卡片底部居中」「弹框底部右」等精确标注

---

### 4.1 学生首页（StudentHome）

**学段可见性**：K ✅核心 / P ✅核心 / J ✅核心

#### 4.1.1 业务逻辑
首次进入 `/student` 时载入；读取 `student_settings.stage` 决定学段徽标显示；从 `student_homework`/`student_review`/`student_achievements` 聚合今日待办数；三屏轮播骨架默认第 1 屏 active；轮播定时器 6 秒切换，鼠标进入暂停，离开恢复。

#### 4.1.2 交互逻辑
- 页面加载 → 显示问候条（按本地小时：5-11 早上好 / 12-13 中午好 / 14-17 下午好 / 18-22 晚上好 / 其他 夜深了）+ 学生昵称 + 今日待办数徽标
- 点击行动台卡片（如「待办作业 3 项」）→ 跳转 `/student?menu=homework`
- 点击工具栏入口（番茄钟/课程表/勋章墙）→ 跳转对应菜单
- 点击右上角学段徽标 → 打开学段切换弹框
- 点击右下角箭头 ▶ → 切换下一屏；◀ → 上一屏
- 点击底部圆点 ●○○ → 跳转指定屏

#### 4.1.3 规则约束
- 学生昵称：1-12 code point，缺失时回退「同学」
- 轮播屏数恒为 3，菜单关闭某项时该屏对应卡片隐藏但屏数不变
- 学段徽标颜色：K=橙、P=蓝、J=紫
- 自动轮播：6 秒 ± 0.5 秒；hover 暂停；离开后 2 秒内不恢复（防误触）

#### 4.1.4 权限逻辑
- 三学段全可见；家长模式下右上角追加「家长」入口
- 行动台卡片可见性跟随学段菜单可见性（如 J 段无习惯卡片）

#### 4.1.5 边界异常
- 学生设置未初始化 → 强制弹框选择学段（不可跳过）
- 待办数据为空 → 行动台显示「今日暂无任务，去添加一项吧」+ 跳转按钮
- 轮播定时器在面板切换/路由离开时清除

#### 4.1.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│ 早上好，小明同学！今日待办 3 项                    [P] [⚙ 设置]   │  ← 顶部：问候条（左）/ 学段徽标+设置（右上）
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│         [ 行动台  · 数据概览 · 工具栏 ]                          │  ← 顶部居中：屏切换 tabs（带下划线 active）
│                                                                 │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │ 待办作业 3 项 │ → 跳转           │ 待复习 5 个    │ → 跳转      │
│  │              │                  │              │            │
│  │ [去完成]      │                  │ [去复习]      │            │  ← 屏 1 内容区：3 张行动卡（点击卡片整体可跳转）
│  └──────────────┘ └──────────────┘ └──────────────┘            │
│                                                                 │
│                                                                 │
│  ◀                                              ▶                │  ← 底部左右居中：左右翻页箭头
│                                                                 │
│                  ●  ○  ○                                       │  ← 底部居中：3 个圆点（当前屏实心）
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 问候条：顶部左对齐
- 学段徽标 [P]：顶部右上角第 1 位
- 设置入口 [⚙ 设置]：顶部右上角第 2 位
- 屏切换 tabs：顶部居中（行动台/数据概览/工具栏）
- 行动卡片整体可点击：屏 1 内容区横排 3 张
- 卡片内「去完成」/「去复习」按钮：卡片底部居中
- 翻页箭头 ◀ ▶：底部左右两端居中
- 圆点指示器：底部水平居中

---

### 4.2 习惯打卡（StudentHabits）

**学段可见性**：K ✅核心 / P ✅核心 / J ⚪可选

#### 4.2.1 业务逻辑
复用 `habits` store（IDB store 'habits'）；学段切换时若 `student_settings.stageHabitsSeeded !== stage` 触发默认习惯播种（K 7 项 / P 6 项 / J 5 项），播种后写入 `stageHabitsSeeded`；每日打卡写入 `records[]`；连续天数从 records 计算；勋章解锁由 StudentAchievements 监听 habits store 变化触发。

#### 4.2.2 交互逻辑
- 点击左侧分类 tab（全部/生活/学习/运动）→ 右侧卡片按分类筛选
- 点击「+ 新增习惯」按钮（顶部右）→ 打开新增弹框
- 点击卡片主体 → 打开编辑弹框
- 点击卡片右下角「打卡」按钮 → 写入今日打卡记录，按钮变灰「已打卡」
- 点击日历热力图某天 → 弹框显示当日打卡详情
- 点击连续天数进度环 → 跳转勋章墙

#### 4.2.3 规则约束
- 习惯名：1-15 code point，必填，同分类内唯一
- 分类：内置 3 类（life/study/exercise），用户可增删自定义分类（被引用禁删）
- 打卡记录：date 本地 'YYYY-MM-DD' 唯一，重复点击取消打卡
- 连续天数计算：以本地今日为基准向前回溯
- 一屏布局：桌面 ≥769px 卡片网格 `repeat(5, minmax(0,1fr))`，行高 82px+2，maxRows=2（10 卡/页）；移动端单列

#### 4.2.4 权限逻辑
- 三学段可见（J 默认隐藏可开启）
- 家长模式下可代为打卡（标记 `parentMarked: true`）

#### 4.2.5 边界异常
- 当日已打卡 → 打卡按钮 disabled，显示「✓ 已打卡」
- 习惯名重复 → toast「该分类下已存在同名习惯」
- 连续天数断档 → 进度环显示 0%，文案「今天重新开始吧」
- 卡片网格数据为空 → 显示空状态「还没有习惯，去添加一项吧」+ 按钮跳转新增

#### 4.2.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  习惯打卡                                          [+ 新增习惯]   │  ← 顶部：标题（左）/ 新增按钮（右上）
├─────────────────────────────────────────────────────────────────┤
│  [全部] [生活] [学习] [运动] [自定义...]                            │  ← 分类 tabs（左对齐）
├─────────────────────────────────────────────────────────────────┤
│  连续天数 7 天 ▓▓▓▓▓▓▓░░░  70%         📅 日历热力图 [展开]     │  ← 顶部统计条（左：进度环 / 右：日历入口）
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐  │
│  │ 🦷 刷牙  │ │ 🛏 早睡  │ │ 📖 阅读  │ │ ⚽ 运动  │ │ 🎒 收拾  │  │  ← 卡片网格（5 列 × 2 行 = 10 卡/页）
│  │ 连续 7天 │ │ 连续 5天 │ │ 连续 3天 │ │ 连续 0天 │ │ 连续 0天 │  │
│  │  [打卡]  │ │ [已打卡] │ │  [打卡]  │ │  [打卡]  │ │  [打卡]  │  │  ← 卡片底部居中：打卡按钮
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘  │
│  ┌─────────┐ ┌─────────┐                                       │
│  │ ...     │ │ ...     │                                       │
│  └─────────┘ └─────────┘                                       │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 2 页  [下一页 →]                 │  ← 底部居中：分页条
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 新增习惯按钮：顶部右上角
- 分类 tabs：分类区左对齐
- 进度环：统计条左侧
- 日历热力图入口：统计条右侧
- 打卡按钮：每张卡片底部居中
- 分页按钮：底部水平居中

---

### 4.3 作业管理（StudentHomework）

**学段可见性**：K ❌ / P ✅核心 / J ✅核心

#### 4.3.1 业务逻辑
独立 store `student_homework`，IDB store 'student_homework'，作业数组，`hw_` 前缀；学段切换时若学科未播种则注入默认学科（小学 3 科 / 初中 9 科）；学科注册表存 `student_settings.subjects`，用户可增删自定义；状态自动流转：到期日 < 今日且未完成 → 'overdue'。

#### 4.3.2 交互逻辑
- 点击学科 tab（全部/语文/数学/.../自定义）→ 按学科筛选
- 点击状态分组 tab（待办/进行中/已完成/逾期）→ 双维度筛选
- 点击「+ 新增作业」按钮（顶部右）→ 新增弹框
- 点击卡片主体 → 编辑弹框
- 点击卡片右下角状态切换按钮 → 状态流转弹框（pending→doing→done）
- 点击卡片左下角学科徽标 → 跳转该学科筛选

#### 4.3.3 规则约束
- 标题：1-50 字符，必填
- 学科：必选，下拉来自 `student_settings.subjects`
- 截止日期：必填，本地 'YYYY-MM-DD'，不可早于今天（编辑时不可改为早于创建日）
- 优先级：low/normal/high，默认 normal
- 状态：pending/doing/done/overdue，done 不可回退
- 一屏布局：5 列网格，行高 158px+2，maxRows=3（15 卡/页）

#### 4.3.4 权限逻辑
- K 段隐藏菜单
- 家长模式下可代为新增/编辑作业（标记来源）

#### 4.3.5 边界异常
- 已完成作业到期 → 不变 overdue
- 学科被作业引用时删除 → 拒绝并提示「该学科下有 N 项作业」
- 卡片为空 → 空状态「今日无作业，去添加一项」+ 跳转按钮
- 截止日期已过未完成 → 卡片左上角红色「逾期」徽标

#### 4.3.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  作业管理                                          [+ 新增作业]   │  ← 顶部：标题（左）/ 新增（右上）
├─────────────────────────────────────────────────────────────────┤
│  学科：[全部] [语文] [数学] [英语] [+]                            │  ← 学科 tabs（左）+ 新增学科（+）
│  状态：[待办] [进行中] [已完成] [逾期]                              │  ← 状态 tabs
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────┐ ┌──────┐  │
│  │[语文] 第3课生字│ │[数学] P12练习 │ │[逾期] 英语背诵│ │ ... │ │ ... │  │  ← 卡片网格 5 列
│  │ ⏰ 8/30 截止   │ │ ⏰ 8/31 截止   │ │ ⏰ 8/28 已过   │ │     │ │     │  │
│  │ [pending]     │ │ [doing]       │ │ [overdue]    │ │     │ │     │  │
│  │              │ │              │ │              │ │     │ │     │  │
│  │  [→ 标记完成]  │ │  [→ 标记完成]  │ │  [→ 标记完成]  │ │     │ │     │  │  ← 卡片底部居中：状态切换按钮
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────┘ └──────┘  │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 2 页  [下一页 →]                 │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 新增作业：顶部右上角
- 学科 tabs + 新增学科 [+]：学科筛选区
- 状态 tabs：状态筛选区
- 学科徽标：卡片左上角
- 截止日期/状态：卡片中部
- 状态切换按钮：卡片底部居中
- 分页按钮：底部水平居中

---

### 4.4 课程表（StudentTimetable）

**学段可见性**：K ❌ / P ✅ / J ✅

#### 4.4.1 业务逻辑
单对象 IDB store 'student_timetable'，结构 `{version, weeks, schedule}`；学段默认节数（小学 6 节/天，初中 8 节/天）；今日课程由本地日期匹配 dayOfWeek；下一节提醒由当前时间匹配 startHHMM。

#### 4.4.2 交互逻辑
- 默认周视图，点击右上角「日视图」切换 → 显示今日课程 + 下一节高亮
- 点击单元格 → 编辑弹框（学科/老师/教室/起止时间）
- 点击「批量复制」按钮（顶部右）→ 选择源日期 + 目标日期范围 → 复制
- 点击「重置课表」按钮（设置弹框内）→ 二次确认清空

#### 4.4.3 规则约束
- 节数：1-12 节/天，可调整
- 学科：必选下拉，来自 `student_settings.subjects`
- 起止时间：HH:MM 格式，endHHMM > startHHMM
- 教室/老师：选填，1-20 字符
- 周视图：7 列（周一到周日），表头本地化「周一/.../周日」

#### 4.4.4 权限逻辑
- K 段隐藏
- 家长模式下可编辑

#### 4.4.5 边界异常
- 单元格为空 → 显示淡灰「+ 添加」可点击新增
- 同一节次同一日重复填写 → 编辑覆盖
- 今日无课 → 日视图显示「今天没有课程」

#### 4.4.6 原型线框图（周视图）

```
┌─────────────────────────────────────────────────────────────────┐
│  课程表                                  [日视图] [批量复制]      │  ← 顶部：标题（左）/ 视图切换+复制（右上）
├──────┬──────┬──────┬──────┬──────┬──────┬──────┬──────┬────────┤
│ 节次  │ 周一 │ 周二 │ 周三 │ 周四 │ 周五 │ 周六 │ 周日 │        │  ← 表头
├──────┼──────┼──────┼──────┼──────┼──────┼──────┼──────┤        │
│ 1    │ 语文 │ 数学 │ 英语 │ 语文 │ 数学 │      │      │        │  ← 今日列高亮（黄色边框）
│ 8:00 │ 张师 │ 李师 │ 王师 │ 张师 │ 李师 │  +   │  +   │        │
├──────┼──────┼──────┼──────┼──────┼──────┼──────┼──────┤        │
│ 2    │ 数学 │ 语文 │ 数学 │ 英语 │ 语文 │  +   │  +   │        │
│ 9:00 │      │      │      │      │      │      │      │        │
├──────┼──────┼──────┼──────┼──────┼──────┼──────┼──────┤        │
│ ...  │ ...  │ ...  │ ...  │ ...  │ ...  │ ...  │ ...  │        │
└──────┴──────┴──────┴──────┴──────┴──────┴──────┴──────┴────────┘
```

**按钮位置清单**：
- 日视图切换：顶部右上角第 1 位
- 批量复制：顶部右上角第 2 位
- 单元格「+ 添加」：空单元格内居中
- 今日列高亮：周列表黄色边框（非按钮）

---

### 4.5 学习计划（StudentPlan）

**学段可见性**：K ❌ / P ⚪简化 / J ✅核心

#### 4.5.1 业务逻辑
IDB store 'student_plans'，数组，`pl_` 前缀；类型 weekly/monthly/term；目标列表 `goals[]` 含进度 0-100；P 段默认隐藏 weekly 之外类型；J 段全开。

#### 4.5.2 交互逻辑
- 点击类型 tab（周计划/月计划/学期计划）→ 筛选
- 点击「+ 新建计划」按钮（顶部右）→ 新建弹框（选类型 + 起止日期 + 目标）
- 点击卡片主体 → 详情页（目标列表 + 进度条 + 复盘编辑）
- 点击目标进度条右侧「+ 进度」按钮 → 输入新进度值
- 点击「复盘」按钮（卡片底部）→ Markdown 编辑器

#### 4.5.3 规则约束
- 起止日期：必填，startDate < endDate
- 目标内容：1-100 字符，必填
- 目标进度：0-100 整数，递增不递减
- 复盘：Markdown，最长 5000 字符
- 一屏布局：5 列网格，行高 192px+2，maxRows=2（10 卡/页）

#### 4.5.4 权限逻辑
- K 段隐藏；P 段仅 weekly 类型；J 段全开
- 家长模式只读

#### 4.5.5 边界异常
- 当日超出计划周期 → 卡片置灰显示「已结束」
- 目标全部完成 → 自动置 done，进度 100%
- 复盘未填写 → 「复盘」按钮红色提示「待复盘」

#### 4.5.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  学习计划                                       [+ 新建计划]      │
├─────────────────────────────────────────────────────────────────┤
│  [周计划] [月计划] [学期计划]                                     │
├─────────────────────────────────────────────────────────────────┤
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐        │
│  │ 第 1 周计划     │ │ 8 月计划         │ │ 初三上学期        │        │
│  │ 8/26-9/1       │ │ 8/1-8/31       │ │ 9/1-1/20       │        │
│  │ ───────────     │ │ ───────────     │ │ ───────────     │        │
│  │ 数学 80% ▓▓▓▓░ │ │ 语文 60%       │ │ 物理 30%       │        │
│  │ 英语 50% ▓▓░░░ │ │ 数学 100% ✓    │ │ 化学 10%       │        │
│  │              │ │              │ │              │        │
│  │  [复盘] [+进度]│ │  [复盘] [+进度]│ │  [复盘] [+进度]│        │  ← 卡片底部居中：复盘+进度按钮
│  └────────────────┘ └────────────────┘ └────────────────┘        │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 1 页  [下一页 →]                 │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 新建计划：顶部右上角
- 类型 tabs：筛选区左对齐
- 复盘按钮 + 进度按钮：卡片底部居中（并列）

---

### 4.6 复习计划（StudentReview）

**学段可见性**：K ❌ / P ❌ / J ✅核心

#### 4.6.1 业务逻辑
IDB store 'student_review'，数组，`rv_` 前缀；艾宾浩斯 6 阶段（1/2/4/7/15/30 天）；今日待复习由 `nextReviewDate === today` 筛选；复习完成 → stage++，nextReviewDate = today + INTERVALS[stage]；stage === 6 → mastered=true 停止提醒。

#### 4.6.2 交互逻辑
- 默认显示「今日待复习」视图，按学科分组
- 点击学科 tab → 切换学科筛选
- 点击条目右侧「已复习」按钮 → 自动推进阶段 + nextReviewDate 更新
- 点击「已掌握」按钮 → 标记 mastered，从待复习列表移除
- 点击「查看日历」按钮（顶部右）→ 复习日历视图，每日条目数

#### 4.6.3 规则约束
- 知识点：1-200 字符，必填
- 学科：必选下拉
- 初学日期：必填本地日期，不可晚于今天
- 阶段：1-6 整数，递增
- 复习日期：自动计算，用户不可直接编辑

#### 4.6.4 权限逻辑
- J 段独有；P 段隐藏；K 段隐藏
- 家长模式只读

#### 4.6.5 边界异常
- 今日无待复习 → 显示「今天没有复习任务，继续保持」+ 历史复习统计
- 已掌握后用户误操作恢复 → 二次确认
- 初学日期未来 → 拒绝并提示「初学日期不可晚于今天」

#### 4.6.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  复习计划                              [今日] [日历] [全部条目]   │  ← 顶部：标题（左）/ 视图切换（右上）
├─────────────────────────────────────────────────────────────────┤
│  📊 今日待复习 5 项 · 累计已掌握 23 项                              │  ← 顶部统计条（左对齐）
├─────────────────────────────────────────────────────────────────┤
│  [全部] [语文] [数学] [英语] [物理] [化学] [...]                  │  ← 学科 tabs
├─────────────────────────────────────────────────────────────────┤
│  ── 语文（2 项）──                                                │  ← 学科分组标题
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 📘 古诗词鉴赏·第 3 章        来源：教材 P45   阶段 3/6      │    │  ← 条目卡片
│  │ 初学：8/20   下次复习：8/30  今天                          │    │
│  │                                [已复习→] [标记已掌握]        │    │  ← 卡片底部右对齐：两个动作按钮
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 📘 文言文虚词                 来源：错题本     阶段 2/6      │    │
│  │ 初学：8/26   下次复习：8/30  今天                          │    │
│  │                                [已复习→] [标记已掌握]        │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ── 数学（3 项）──                                                │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ ...                                                       │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 视图切换（今日/日历/全部）：顶部右上角
- 学科 tabs：筛选区左对齐
- 「已复习→」按钮：条目卡片底部右对齐第 1 位
- 「标记已掌握」按钮：条目卡片底部右对齐第 2 位

---

### 4.7 错题本（StudentMistakes）

**学段可见性**：K ❌ / P ⚪可选 / J ✅核心

#### 4.7.1 业务逻辑
IDB store 'student_mistakes'，数组，`mk_` 前缀；图片 Blob 存独立 store 'student_images'，导出时 base64 编码；状态 new/reviewing/mastered；与复习计划联动：录入后可选加入复习计划。

#### 4.7.2 交互逻辑
- 点击学科 tab + 标签 tab（双维度筛选）
- 点击「+ 新增错题」按钮（顶部右）→ 新增弹框
- 点击卡片主体 → 详情页（题干 + 图片 + 答案 + 分析 + 标签）
- 点击卡片右下角「加入复习」按钮 → 弹框确认 → 写入 student_review
- 点击「拍照」按钮（新增弹框内）→ 调起摄像头/上传

#### 4.7.3 规则约束
- 题干：Markdown，必填（或图片必填）
- 学科：必选下拉
- 标签：0-10 个，每个 1-20 字符
- 正确答案：必填 Markdown
- 状态：new → reviewing（加入复习后） → mastered
- 图片：单张 ≤ 200KB，单题最多 5 张

#### 4.7.4 权限逻辑
- P 段默认隐藏可开启；J 段默认显示
- 家长模式只读

#### 4.7.5 边界异常
- 题干和图片都为空 → 拒绝并提示
- 图片压缩失败 → 保留原图并 toast「图片较大可能影响性能」
- 已加入复习的错题 → 「加入复习」按钮置灰「已加入」

#### 4.7.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  错题本                                            [+ 新增错题]   │  ← 顶部：标题（左）/ 新增（右上）
├─────────────────────────────────────────────────────────────────┤
│  学科：[全部] [语文] [数学] [英语] [物理] [...]                    │
│  标签：[全部] [函数] [几何] [文言文] [+]                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────┐ ┌──────┐  │
│  │ [数学] 几何   │ │ [语文] 文言文 │ │ [英语] 完形  │ │ ... │ │ ... │  │  ← 卡片网格 5 列
│  │ 📷 图片       │ │ 📝 文字       │ │ 📷 图片       │ │     │ │     │  │
│  │ [new]        │ │ [reviewing]  │ │ [mastered]   │ │     │ │     │  │
│  │              │ │              │ │              │ │     │ │     │  │
│  │ [加入复习]   │ │ [已加入]      │ │ [已掌握]      │ │     │ │     │  │  ← 卡片底部居中：加入复习按钮
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────┘ └──────┘  │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 3 页  [下一页 →]                 │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 新增错题：顶部右上角
- 学科 tabs + 标签 tabs + 新增标签 [+]：双筛选区
- 加入复习按钮：卡片底部居中（已加入/已掌握置灰）

---

### 4.8 阅读记录（StudentReading）

**学段可见性**：K ✅亲子 / P ✅ / J ✅

#### 4.8.1 业务逻辑
IDB store 'student_reading'，数组，`rd_` 前缀；累计页数/时长从 entries 聚合；折线图近 30 天阅读时长；小学低段需家长签字（K 段强制 / P 1-3 年级可选 / 其他学段无）。

#### 4.8.2 交互逻辑
- 点击「+ 记录阅读」按钮（顶部右）→ 新增弹框（书名/页数/时长/感悟）
- 点击卡片主体 → 编辑弹框
- 点击卡片左下角「家长签字」按钮（P 低段）→ 弹框输入家长 PIN → 签字
- 点击顶部统计卡 → 切换折线图（页数/时长）

#### 4.8.3 规则约束
- 书名：1-50 字符，必填
- 页数：1-999，必填正整数
- 时长：1-480 分钟，必填
- 感悟：Markdown，最长 2000 字符
- 阅读日期：本地 'YYYY-MM-DD'，必填，不可晚于今天
- 一屏布局：5 列网格，行高 158px+2，maxRows=3（15 卡/页）

#### 4.8.4 权限逻辑
- 三学段可见
- K 段家长签字强制；P 1-3 年级可选；其他无签字
- 家长模式下签字无需再输 PIN

#### 4.8.5 边界异常
- 同日多条记录 → 允许（多次阅读）
- 折线图无数据 → 显示「开始你的第一次阅读吧」+ 跳转按钮
- 总页数 < 1 → 不显示统计

#### 4.8.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  阅读记录                                      [+ 记录阅读]       │  ← 顶部：标题（左）/ 新增（右上）
├─────────────────────────────────────────────────────────────────┤
│  📚 累计 12 本 · 320 页 · 18 小时                                  │  ← 顶部统计条
│  ┌────────────────────────────────────────────────────────┐     │
│  │       近 30 天阅读时长折线图                            │     │  ← 折线图区
│  │  [页数] [时长]                                          │     │  ← 图表切换 tabs（右上）
│  └────────────────────────────────────────────────────────┘     │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────┐ ┌──────┐  │
│  │ 📖 西游记      │ │ 📖 童年        │ │ 📖 三体        │ │ ... │ │ ... │  │
│  │ 8/29 · 30页   │ │ 8/28 · 20页   │ │ 8/27 · 50页   │ │     │ │     │  │
│  │ 60 分钟       │ │ 45 分钟       │ │ 90 分钟       │ │     │ │     │  │
│  │ ✓ 家长已签    │ │ ⚠ 待家长签字   │ │  ─            │ │     │ │     │  │
│  │              │ │              │ │              │ │     │ │     │  │
│  │  [家长签字]   │ │  [家长签字]   │ │  ─            │ │     │ │     │  │  ← 卡片底部居中：家长签字按钮（K/P低段）
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────┘ └──────┘  │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 2 页  [下一页 →]                 │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 记录阅读：顶部右上角
- 图表切换 tabs：折线图区右上角
- 家长签字按钮：卡片底部居中（K/P 低段独有，已签字置灰）

---

### 4.9 考试倒计时（StudentExam）

**学段可见性**：K ❌ / P ⚪可选 / J ✅核心

#### 4.9.1 业务逻辑
复用 `countdowns` store（IDB store 'countdowns'）；通过自定义分类 `exam` 区分；学段切换播种默认考试（小学期中/期末；初中加月考；初三加中考）；到期触发复用 useCountdownReminder 三通道（弹框+桌面通知+邮件）。

#### 4.9.2 交互逻辑
- 点击「+ 新增考试」按钮（顶部右）→ 新增弹框（学科/范围/目标分数 + 复用倒计时不重复字段）
- 点击卡片主体 → 编辑弹框
- 点击卡片右下角「开始复习」按钮 → 跳转复习计划面板，预筛选该学科
- 点击分类 tab（全部/期中/期末/月考/中考）

#### 4.9.3 规则约束
- 标题：1-50 字符，必填
- 学科：选填（全科考试留空）
- 目标分数：0-750 整数（兼容中考满分），选填
- 日期：必填本地日期，可未来可过去（过去显示「已结束」）
- 一屏布局：5 列网格，行高 158px+2，maxRows=3（15 卡/页）

#### 4.9.4 权限逻辑
- K 段隐藏；P 段默认隐藏可开启；J 段默认显示
- 家长模式下可代为新增

#### 4.9.5 边界异常
- 距考试 < 1 天 → 卡片红色边框 + 「明天考试加油」
- 距考试已过 → 灰色「已结束」+ 显示实际距今天数
- 同日多个考试 → 允许，并列展示

#### 4.9.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  考试倒计时                                      [+ 新增考试]     │  ← 顶部：标题（左）/ 新增（右上）
├─────────────────────────────────────────────────────────────────┤
│  [全部] [期中] [期末] [月考] [中考]                                │  ← 分类 tabs
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────┐ ┌──────┐  │
│  │ 📅 期中考试    │ │ 📅 期末考试    │ │ 📅 月考        │ │ ... │ │ ... │  │
│  │ 11/10 周一    │ │ 1/15 周三     │ │ 9/25 周四     │ │     │ │     │  │
│  │ ⏰ 还有 60 天  │ │ ⏰ 还有 130 天 │ │ ⏰ 还有 25 天  │ │     │ │     │  │
│  │ 范围：1-4章    │ │ 范围：全部     │ │ 范围：函数     │ │     │ │     │  │
│  │ 目标：600 分   │ │ 目标：650 分   │ │ 目标：90 分    │ │     │ │     │  │
│  │              │ │              │ │              │ │     │ │     │  │
│  │  [开始复习]   │ │  [开始复习]   │ │  [开始复习]   │ │     │ │     │  │  ← 卡片底部居中：开始复习按钮
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────┘ └──────┘  │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 1 页  [下一页 →]                 │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 新增考试：顶部右上角
- 分类 tabs：筛选区左对齐
- 开始复习按钮：卡片底部居中

---

### 4.10 学习日记（StudentDiary）

**学段可见性**：K ⚪涂鸦 / P ✅ / J ✅

#### 4.10.1 业务逻辑
复用 `diary` store（IDB store 'diary'，存 `{entries}` 对象非数组）；每日一篇，date 本地 'YYYY-MM-DD' 唯一；学段模板 K=涂鸦/P=拼音短句/J=完整 Markdown；排序走 diaryCore。

#### 4.10.2 交互逻辑
- 点击「+ 今日日记」按钮（顶部右）→ 编辑器（已存在今日则打开编辑）
- 点击历史卡片 → 编辑模式
- 点击模板按钮（编辑器顶部）→ 插入模板
- K 段点击「家长代记」按钮（编辑器右下）→ 输入家长 PIN 后切换家长输入模式

#### 4.10.3 规则约束
- date：本地 'YYYY-MM-DD'，唯一
- content：Markdown，最长 10000 字符，trim 空跳过不保存
- id：`dy_` 前缀，缺失回退 `dy_<date>`
- 一屏布局：行高 192px+2，分页 PanelPager

#### 4.10.4 权限逻辑
- 三学段可见；K 段强制家长代记
- 家长模式下可编辑孩子日记

#### 4.10.5 边界异常
- 今日已写 → 「+ 今日日记」按钮变「编辑今日」
- 保存时空内容 → 拒绝并提示
- 历史无日记 → 空状态「还没有日记，记录今天吧」+ 跳转按钮

#### 4.10.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  学习日记                              [+ 今日日记] / [编辑今日]   │  ← 顶部：标题（左）/ 新增按钮（右上）
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 📝 今日日记                              [模板] [预览]      │    │  ← 编辑器顶部：模板+预览按钮（右上）
│  │ ────────────────────────────────────────────────────── │    │
│  │ 今天学习了...                                          │    │
│  │ 掌握的知识点：                                          │    │
│  │ 需要改进的地方：                                        │    │
│  │ 明日计划：                                              │    │
│  │                                                        │    │
│  │                              [取消]   [保存]            │    │  ← 编辑器底部右对齐：取消+保存
│  └─────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│  历史日记                                                        │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │ 📅 8/29 周四  │ │ 📅 8/28 周三  │ │ 📅 8/27 周二  │            │  ← 历史卡片
│  │ 今天学习了函数... │ │ ...           │ │ ...           │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│              [← 上一页]  第 1 / 3 页  [下一页 →]                 │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 今日日记/编辑今日按钮：顶部右上角
- 模板/预览按钮：编辑器顶部右上
- 取消/保存按钮：编辑器底部右对齐（取消左，保存右）
- 分页按钮：底部水平居中

---

### 4.11 番茄钟（StudentPomodoro）

**学段可见性**：K ❌ / P ⚪可选 / J ✅

#### 4.11.1 业务逻辑
复用 `pomodoro` store；学段默认时长（小学 25+5 / 初中 50+10）；今日累计统计从 records 聚合；连续专注徽章由 achievements 监听。

#### 4.11.2 交互逻辑
- 点击中央大圆按钮「开始专注」→ 启动计时
- 启动后按钮变「暂停」「停止」（左右两侧）
- 选择学科下拉（顶部）+ 关联作业/复习下拉（顶部）
- 完成一个番茄 → 弹框「休息 5 分钟」+ 自动切换休息计时
- 点击「今日统计」按钮（顶部右）→ 弹框显示今日累计 + 历史趋势

#### 4.11.3 规则约束
- 专注时长：5-120 分钟，默认按学段
- 休息时长：1-30 分钟，默认按学段
- 关联作业/复习：选填，完成后可标记作业进度
- 中断专注 → 不计入今日累计

#### 4.11.4 权限逻辑
- K 段隐藏；P 段默认隐藏可开启；J 段默认显示
- 家长模式下可配置学段时长

#### 4.11.5 边界异常
- 关闭浏览器中断 → 下次进入提示「上次专注未完成，是否继续」
- 学科未选择 → 允许但不计入学科统计

#### 4.11.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  番茄钟              [学科: 数学 ▼] [关联: 第3课作业 ▼] [今日统计] │  ← 顶部：标题（左）/ 学科+关联+统计（右上）
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                                                                 │
│                      ┌──────────────┐                            │
│                      │              │                            │
│                      │   25:00      │                            │  ← 中央大圆按钮（计时显示）
│                      │              │                            │
│                      └──────────────┘                            │
│                                                                 │
│                  [停止]  [开始专注]  [暂停]                       │  ← 中央圆下方：三按钮横排（启动前仅「开始专注」）
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  📊 今日专注 4 个 · 100 分钟   本周 18 个 · 7.5 小时              │  ← 底部统计条
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 学科下拉：顶部右上角第 1 位
- 关联下拉：顶部右上角第 2 位
- 今日统计按钮：顶部右上角第 3 位
- 中央大圆按钮：页面水平+垂直居中
- 停止/开始专注/暂停按钮：中央圆下方水平居中（启动前仅显示「开始专注」）

---

### 4.12 成就勋章墙（StudentAchievements）

**学段可见性**：K ✅核心 / P ✅核心 / J ⚪可选

#### 4.12.1 业务逻辑
IDB store 'student_achievements'，单对象 `{definitions, unlocked}`；定义内置 10 枚（streak-7/30/100、book-10/50/100、pomo-50/200/500、hw-rate-95）；解锁由监听各 store 变化触发（habits/homework/reading/pomodoro）。

#### 4.12.2 交互逻辑
- 默认显示全部勋章墙网格
- 点击分类 tab（全部/习惯/学习/阅读/专注）→ 筛选
- 点击已解锁勋章 → 弹框显示解锁时间 + 详情
- 点击未解锁勋章 → 弹框显示进度条 + 「如何解锁」说明

#### 4.12.3 规则约束
- 勋章定义不可编辑（内置）
- 解锁自动触发，不可手动撤销
- 进度计算从对应 store 实时聚合

#### 4.12.4 权限逻辑
- K/P 段默认显示；J 段默认隐藏可开启
- 家长模式下可手动发放特殊勋章（手动解锁）

#### 4.12.5 边界异常
- 全部解锁 → 顶部追加「全部解锁！等待新勋章上线」
- 数据为空（无 habits/reading 等）→ 全部勋章 0% 进度

#### 4.12.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  成就勋章墙                                                      │  ← 顶部：标题（无新增按钮）
├─────────────────────────────────────────────────────────────────┤
│  🏆 已解锁 5 / 10                                                │  ← 顶部统计
├─────────────────────────────────────────────────────────────────┤
│  [全部] [习惯] [学习] [阅读] [专注]                                │  ← 分类 tabs
├─────────────────────────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                  │
│  │ 🔥   │ │ 🌟   │ │ 🏆   │ │ 📚   │ │ 📖   │                  │  ← 勋章网格 5 列
│  │ 一周  │ │ 月度  │ │ 百日  │ │ 阅读新│ │ 阅读达│                  │
│  │ 坚持  │ │ 坚持  │ │ 坚持  │ │ 秀    │ │ 人    │                  │
│  │ ✅    │ │ ✅    │ │ 0/100 │ │ 5/10  │ │ 0/50  │                  │
│  │ 已解锁│ │ 已解锁│ │ ▓░░░  │ │ ▓░░░  │ │ ▓░░░  │                  │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘                  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                  │
│  │ 🎓   │ │ ⏰   │ │ ⚡   │ │ 💎   │ │ ✅   │                  │
│  │ 阅读大│ │ 专注新│ │ 专注达│ │ 专注大│ │ 作业标│                  │
│  │ 师    │ │ 手    │ │ 人    │ │ 师    │ │ 兵    │                  │
│  │ 0/100 │ │ 0/50  │ │ 0/200 │ │ 0/500 │ │ 80%   │                  │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘                  │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 勋章卡片整体可点击：网格内每张
- 分类 tabs：筛选区左对齐
- 无新增按钮（内置定义）

---

### 4.13 奖励积分（StudentRewards）

**学段可见性**：K ✅核心 / P ✅核心 / J ❌

#### 4.13.1 业务逻辑
IDB store 'student_rewards'，单对象 `{totalPoints, history, rewards}`；积分规则：完成习惯+5、完成作业+10、阅读 30 分钟+5、家长手动加分；兑换扣分；交易历史持久化。

#### 4.13.2 交互逻辑
- 点击「+ 兑换奖励」按钮（顶部右）→ 选择奖励项 → 二次确认 → 扣分
- 点击「+ 新增奖励项」按钮（设置弹框内）→ 配置名称+所需积分
- 点击历史交易条目 → 详情弹框
- 点击顶部积分余额 → 切换到「积分规则」说明页

#### 4.13.3 规则约束
- 积分余额：整数 ≥ 0，兑换时不足拒绝
- 奖励项名称：1-30 字符，必填，唯一
- 所需积分：1-9999 正整数
- 库存：选填，0 表示售罄
- 交易历史：保留最近 100 条

#### 4.13.4 权限逻辑
- K/P 段默认显示；J 段隐藏
- 学生可兑换，不可手动加分
- 家长模式下可手动加分/配置奖励项

#### 4.13.5 边界异常
- 积分不足兑换 → 拒绝并提示「积分不足，还差 N 分」
- 奖励项库存为 0 → 卡片置灰「售罄」
- 历史为空 → 显示「还没有交易记录」

#### 4.13.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  奖励积分                                  [+ 兑换奖励]            │  ← 顶部：标题（左）/ 兑换（右上）
├─────────────────────────────────────────────────────────────────┤
│           🪙 当前积分：125 分                  [积分规则]          │  ← 顶部居中：积分余额 / 右侧：规则入口
├─────────────────────────────────────────────────────────────────┤
│  可兑换奖励                                                      │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │ 📺 看 30 分钟  │ │ 🍦 冰激凌      │ │ 🎮 玩游戏 1h   │            │  ← 奖励卡片网格
│  │ 动画          │ │              │ │              │            │
│  │ 30 分         │ │ 50 分         │ │ 100 分        │            │
│  │              │ │              │ │              │            │
│  │  [兑换]       │ │  [兑换]       │ │  [兑换]       │            │  ← 卡片底部居中：兑换按钮
│  └──────────────┘ └──────────────┘ └──────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│  交易历史                                          [查看全部]     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 8/29 +10  完成作业「第3课生字」                          │    │  ← 历史条目列表
│  │ 8/29 +5   完成阅读「西游记」30 分钟                      │    │
│  │ 8/28 -50  兑换「冰激凌」                                 │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

**按钮位置清单**：
- 兑换奖励按钮：顶部右上角
- 积分规则入口：顶部居中右侧（紧邻积分余额）
- 兑换按钮：每张奖励卡片底部居中
- 查看全部按钮：历史区右上角

---

### 4.14 家长协同（StudentParent）

**学段可见性**：K ✅核心 / P ✅ / J ❌

#### 4.14.1 业务逻辑
家长入口独立 PIN（PBKDF2 + salt 存 `student_settings.parentPin`）；解锁后进入家长模式；家长可设置每日任务清单、查看孩子完成情况报告、手动加分、发放特殊勋章、阅读签字、配置奖励项。

#### 4.14.2 交互逻辑
- 点击右上角学段徽标旁「家长」入口 → PIN 输入弹框
- 解锁成功 → 顶部追加「家长模式」横条 + 右上角「退出家长模式」按钮
- 家长模式下原卡片追加「家长操作」按钮组（手动加分、签字、代为打卡）
- 点击「设置每日任务」按钮（家长横条左）→ 弹框配置当日任务清单
- 点击「孩子报告」按钮（家长横条中）→ 弹框显示今日/本周完成情况

#### 4.14.3 规则约束
- PIN：4-8 位数字，不可为全 0 或全 1
- PIN 输错 5 次 → 锁定 5 分钟
- 重置 PIN：需主密码二次验证（类比密码库解锁）

#### 4.14.4 权限逻辑
- K/P 段家长入口可见；J 段隐藏
- 家长模式可执行：手动加分、签字、代打卡、配置奖励项、设置每日任务

#### 4.14.5 边界异常
- PIN 未设置 → 弹框引导设置（首次设置无需旧 PIN）
- PIN 锁定 → 显示剩余解锁时间
- 主密码错误 → 拒绝重置 PIN

#### 4.14.6 原型线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠ 家长模式中  [设置每日任务] [孩子报告] [配置奖励]    [退出]      │  ← 家长横条（顶部，仅家长模式显示）
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│              （原面板内容，卡片追加家长操作按钮）                  │
│                                                                 │
│  ┌──────────────┐ ┌──────────────┐                              │
│  │ 🦷 刷牙       │ │ 📖 阅读       │                              │
│  │ 连续 7 天     │ │ 连续 3 天     │                              │
│  │  [打卡]       │ │  [打卡]       │                              │
│  │              │ │              │                              │
│  │ [家长代打卡]  │ │ [家长代打卡]  │                              │  ← 卡片底部追加：家长代打卡按钮
│  └──────────────┘ └──────────────┘                              │
└─────────────────────────────────────────────────────────────────┘

PIN 输入弹框：
┌─────────────────────────────────────────┐
│         家长模式解锁                     │
│                                         │
│   请输入家长 PIN                         │
│   ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐      │
│   │  ●  │ │  ●  │ │  ●  │ │  ●  │      │  ← PIN 输入框（4-8 位）
│   └─────┘ └─────┘ └─────┘ └─────┘      │
│                                         │
│   [取消]              [解锁]            │  ← 弹框底部：取消（左）/ 解锁（右）
│                                         │
│              忘记 PIN？[重置]            │  ← 弹框底部居中：重置入口
└─────────────────────────────────────────┘
```

**按钮位置清单**：
- 家长横条：顶部居中（仅家长模式）
- 设置每日任务 / 孩子报告 / 配置奖励按钮：家长横条内左中右排列
- 退出家长模式：家长横条最右
- 家长代打卡按钮：每张卡片底部追加（紧贴原打卡按钮下方）
- PIN 解锁弹框：取消（左下）/ 解锁（右下）/ 重置入口（底部居中）

---

## 三、共性交互约定

### 3.1 顶部布局规范
所有面板顶部统一为「标题（左）+ 操作按钮组（右上）」结构：
- 标题：左对齐，16-18px 字号
- 操作按钮组：右对齐，主操作（新增/记录）固定第 1 位

### 3.2 筛选 tabs 规范
- 分类/学科/状态 tabs 横排，左对齐
- 新增分类入口 [+] 紧贴 tabs 末尾
- 默认选中「全部」

### 3.3 卡片底部按钮规范
- 主操作按钮（打卡/兑换/标记完成/加入复习）：卡片底部居中
- 次操作按钮（家长代打卡）：紧贴主按钮下方
- 状态徽标（已打卡/已加入/已掌握/逾期）：卡片左上角

### 3.4 分页条规范
- 位置：底部水平居中
- 结构：[← 上一页]  第 X / Y 页  [下一页 →]
- 单页时不渲染分页条
- 边界禁用（第 1 页上一页禁用，末页下一页禁用）

### 3.5 弹框规范
- 新增/编辑弹框：宽 480px，最大高度 90vh，垂直滚动
- 列表弹框（如健康记录）：宽 1000px，5 列 × 3 行 = 15 卡/页
- 底部按钮：取消（左）+ 主操作（右），主操作高亮主色

### 3.6 学段可见性图标
- ✅ 核心：默认显示且推荐
- ✅：默认显示
- ⚪：默认隐藏可开启
- ❌：不支持

---

## 四、按钮位置速查表

| 模块 | 主新增按钮位置 | 卡片主操作位置 | 卡片次操作位置 | 分页条位置 |
|------|---------------|--------------|--------------|-----------|
| 1 学生首页 | 顶部右上（设置入口） | 行动卡整体可点 | 圆点+箭头（底部居中） | 圆点+箭头 |
| 2 习惯打卡 | 顶部右上 | 卡片底部居中（打卡） | — | 底部居中 |
| 3 作业管理 | 顶部右上 | 卡片底部居中（标记完成） | 卡片左上（学科徽标可点） | 底部居中 |
| 4 课程表 | 顶部右上（批量复制） | 单元格内（+添加） | — | — |
| 5 学习计划 | 顶部右上 | 卡片底部居中（复盘+进度） | — | 底部居中 |
| 6 复习计划 | 顶部右上（视图切换） | 卡片底部右对齐（已复习+已掌握） | — | — |
| 7 错题本 | 顶部右上 | 卡片底部居中（加入复习） | — | 底部居中 |
| 8 阅读记录 | 顶部右上 | 卡片底部居中（家长签字） | — | 底部居中 |
| 9 考试倒计时 | 顶部右上 | 卡片底部居中（开始复习） | — | 底部居中 |
| 10 学习日记 | 顶部右上 | 编辑器底部右对齐（保存） | 编辑器顶部右（模板/预览） | 底部居中 |
| 11 番茄钟 | 顶部右上（学科+关联+统计） | 中央圆下方水平居中 | — | — |
| 12 成就勋章 | —（无新增） | 勋章卡片整体可点 | — | — |
| 13 奖励积分 | 顶部右上（兑换） | 卡片底部居中（兑换） | 历史区右上（查看全部） | — |
| 14 家长协同 | 家长横条内（左中右） | PIN 弹框底部右（解锁） | 卡片底部追加（家长代打卡） | — |

---

> **下一步**：本详述文档经评审通过后，按 `PRD-学生工作台.md` M1-M6 里程碑实施。每模块开发前对照本文档的「业务/交互/规则/权限/边界」5 维度 + 原型线框图作为验收基准。
