# 2026-08-29

## 走查结论后的 Admin 侧 emoji 整改（阶段 A+B）— 承接 2026-08-28 走查
用户确认范围为「A + B 全控件」。全量整改已完成并通过验证，仍保持未提交（review 状态）。

### 做法要点（可复用）
- **权威 path 来源**：`npm install @mdi/js` 装到托管 node workspace（`~/.workbuddy/binaries/node/workspace`），用 ESM 脚本提取 `mdiXxx` 的 24×24 path 字面量，再写进 `src/components/Icon.vue`。**不引入项目依赖**，且 path 100% 准确（不靠记忆瞎写）。
- **安全设计（关键）**：批量替换脚本**只改 `</script>` 之后的模板区，不碰 script 区**，避免把 `<Icon>` 组件标签塞进 JS 字符串/数据数组（如 Toast 的 icons 映射、SiteModal 的错误提示串、HelpModal 的 features 数组）导致渲染成字面文本。
- 脚本自动为用到 `<Icon>` 但缺少 import 的文件补 `import Icon from '<相对路径>/Icon.vue'`（components/→`./`，business·workbench/→`../`，views/→`../components/`）。若该文件已有 import（如 WorkbenchSleep 用 `@/components/Icon.vue` 别名）则跳过，不重复添加。
- emoji 后常跟变体选择符 U+FE0F，替换正则统一用 `字符 + \uFE0F?`，避免残留不可见字符。

### 本次成果
- `Icon.vue` 新增 32 个 MDI 图标：close/check/pencil/trash/link/image/download/upload/refresh/folder/lightbulb/info/package/archive/palette/gradient/weather-sunny/weather-night/merge/monitor/document/save/clipboard/cancel/keyboard/bookmark/trending-up/timer-sand/timer/calendar/email。
- 共 **135 处 emoji → SVG**，覆盖 **36 个文件**（阶段 A 装饰标题 + 阶段 B 全站控件 glyph）。
- 特殊结构性改造（非简单替换）：
  - `HelpModal`：features 数组 `icon: '🔗'` → `icon: 'link'` 等 13 项，模板改 `<Icon :name="feature.icon" />`。
  - `BackupManager`：`{{ cond ? '✅ 已开启' : '❌ 已关闭' }}` → `<Icon :name="cond ? 'check' : 'close'" />` + 纯文本（组件标签不能进三元字符串）。
  - `Toast`：icons 映射值改为图标名，模板改 `<Icon :name="icons[toast.type]" />`。
  - `ThemeToggle`：`{{ cond ? '🌙' : '☀️' }}` → `<Icon :name="cond ? 'weather-night' : 'weather-sunny'" />`。
  - `IconManager`：`placeholder="🔍 搜索图标名称..."` 中 emoji 只能删除（属性内不能放组件标签）。
  - `SiteModal`：错误提示字符串中的 ⚠️ 直接移除。
- 顺带修掉 P1-⑤ 遗留隐患：App Bar 内其余按钮已改 ghost，但 `ThemeToggle` 自带白卡样式显得突兀；在 HomeView 用 `.app-bar-right :deep(.theme-toggle)` 收敛为 ghost（含暗色），不动 DisplayView 原样式。

### 验证
`vue-tsc -b` EXIT=0；`vite build`（Temp 临时目录）EXIT=0。遗留告警（INEFFECTIVE_DYNAMIC_IMPORT / chunk>500kB）为历史既有。

### 剩余 emoji（231 处，全部为「emoji 即数据」，属类别 C，需另立图标体系）
- `CategoryManager` 分类图标选择器数组 191 处（最大头）
- 天气：`useWeather.ts` 8 + `WeatherCard.vue` 8
- 工作台统计/置顶：`WorkbenchHabits` 5、`WorkbenchNotes` 4、`WorkbenchDiet` 🍽️、`WorkbenchWeight` 📏、`WorkbenchSleep` ★（星级评分）
- 锚点日历 `CalendarAnchorCard` 3（⏰🎂📅）
- 分类图标数据：`stores/categories.ts` 2、`types/index.ts` 2、`useBackup.ts` 1
- 2 处代码注释里的 ⚙️（BusinessCategoryManager / BusinessProducts，无害）

### 扫描工具
`emoji_scan.py`（Temp）覆盖 U+1F000-1FAFF / 2600-27BF / 2300-23FF / 2B00-2BFF / 25A0-25FF，**需跳过 U+FE0F 变体选择符与 ⌘ / ▸▾ 等 UI 指示器**，否则误报。
