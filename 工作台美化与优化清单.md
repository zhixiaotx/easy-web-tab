# 个人工作台 & 销售记账工作台 — 美化与优化清单

> 范围：代码仓中的两个工作台视图
> - **个人工作台** `src/views/WorkbenchView.vue`（首页 / 待办 / 笔记 / 日记 / 倒计时 / 番茄钟 / 习惯 / 密码 / 健康 / 记账，共 10 模块）
> - **销售记账工作台** `src/views/BusinessView.vue`（首页 / 商品 / 进货 / 收摊 / 支出 / 库存 / 统计，共 7 模块）
>
> 本次聚焦"视觉统一 + 美化"，并给出可继续迭代的优化方向。

---

## 一、本次已实施的美化（已完成 ✅）

### A. 销售记账工作台（原差距最大，重点改造）
| 项 | 改造前 | 改造后 |
|---|---|---|
| 侧边栏图标 | 9+ 个 emoji（🏠📦🛒📋💰📈📊） | 统一内联 SVG（`Icon.vue` 查表，暗色自动继承 `currentColor`） |
| 侧栏折叠 | 无 | 新增折叠开关，对齐个人工作台（展开 200px / 折叠 56px 仅图标，移动端忽略折叠态横排） |
| 首页问候头 | 无 | 新增渐变问候头：问候语 + 当天日期 + 利润主指标 |
| 首页统计卡 | 5 卡纯文字 + emoji | SVG 图标 + 渐变强调 + 悬浮反馈 |
| 卡片标题 | emoji（🏪⚠️📊） | SVG 图标 |
| 子页面残留 emoji | 支出⚙️ / 库存⚠️ / 商品⚙️📊🛒📋 / 分类🏷️🔒💸 | 全部替换为 SVG |

### B. 个人工作台（已较完善，做一致性收尾）
| 面板 | 替换的 emoji → SVG |
|---|---|
| 记账 | 💰收入 / 📉支出 / ⚖️结余 / 🏦存款 / 🧾笔数 / 📊支出比 / 📈图表 |
| 体重 | ⚖️ / 📈 / 🎯(减肥建议) |
| 番茄钟 | ⚙️(时长设置) |
| 密码 | 🔒(锁定) / 📋(复制) |
| 运动 | 🏃 / 睡眠 😴 |
| 首页 | 🔒(解锁后可见) → 锁 SVG |
| 顶部栏 | 两个工作台「设置」⚙️ → cog，「云同步」☁️ → cloud SVG |

### C. 设计系统统一（基础设施）
- `src/components/Icon.vue` 新增图标：`products / purchases / daily / expenses / inventory / stats / revenue / profit / store / alert / cost / cog / lock / tag / scale / bank / target / copy / cloud`（均为 Material Design Icons，Apache-2.0）。
- `src/stores/settings.ts` 新增 `businessSidebarCollapsed` 字段（6 处：类型声明 / ref / 序列化 / 反序列化 / 重置 / setter），与个人工作台 `workbenchSidebarCollapsed` 对称，支持折叠态持久化。
- 所有图标 `fill=currentColor`，暗色主题自动继承，无需为暗色单独配色。

### D. P0 体验闭环（第二轮已实施 ✅）
| 项 | 改造前 | 改造后 |
|---|---|---|
| 业务台统计卡点击跳转 | 5 张统计卡 + 低库存卡纯展示 | 点击下钻：营业额/成本/利润/毛利率 → 统计页；支出 → 支出页；低库存 → 库存页（卡片 `role=button` + `tabindex` + 键盘 Enter，`:focus-visible` 高亮） |
| 业务台模块持久化 | `activeSection` 本地 ref，刷新回首页 | 新增 `businessActiveSection` 字段，存 settings（类型/ref/序列化/反序列化/重置/setter + `BUSINESS_SECTION_KEYS` 白名单校验），刷新保持所在模块 |
| 密码面板空闲自动锁定 | 解锁后一直保持 | 解锁后启动 idle 计时（默认 5 分钟，常量 `IDLE_LOCK_MS`），监听 mousemove/keydown/touchstart/scroll 重置；连续活动节流（>1s 才重置）；超时自动 `handleLock()`；锁定/卸载自动清理监听与定时器 |

> 实施文件：`src/components/business/BusinessHome.vue`（P0-1）、`src/views/BusinessView.vue` + `src/stores/settings.ts` + `src/types/index.ts`（P0-2）、`src/components/workbench/WorkbenchPassword.vue`（P0-3）。

---

## 二、可继续优化的方向（待迭代，非本次实施）

### 优先级 P0 — 体验闭环（已全部完成 ✅，见上 D 节）
1. ~~**业务台首页统计卡可点击跳转**~~ ✅ 已完成
2. ~~**业务台 `activeSection` 持久化**~~ ✅ 已完成
3. ~~**密码面板空闲自动锁定**~~ ✅ 已完成

### 优先级 P1 — 可视化与效率
4. **首页问候头增强**：利润主指标可加"今日 / 本月"切换、环比箭头、目标完成度进度环（复用个人工作台 `progress-ring` 风格）。
5. **销售排行加迷你图**：当前是文字列表，可加每分类/商品的迷你柱状图（零依赖内联 SVG，参考记账趋势图 `ledgerCore`）。
6. **业务台一键导出**：记账 / 库存 / 收摊记录支持导出 CSV（或 Excel），目前仅有个人工作台记账有图表、无导出入口。
7. **个人工作台「今日概览」聚合**：首页 bento 可加一块聚合卡（待办 N 项 / 今日番茄 N 个 / 未读提醒 N 条），减少切换成本。

### 优先级 P2 — 一致性与细节（第三轮已实施 ✅，见下 E 节；仅 ⑫ 超出两个工作台范围未做）
8. ~~**图标尺寸 token 化**~~ ✅ 已完成
9. ~~**无障碍 aria-label**~~ ✅ 已完成（含清掉两个工作台内残留 emoji 👁️/🙈/🔑/👤）
10. ~~**骨架屏 / 加载态**~~ ✅ 已完成
11. ~~**键盘可达性**~~ ✅ 已完成
12. **Admin 侧 emoji 残留**（设置弹窗 ⚙️、备份 📦📋、分类 🔒、帮助 📈🏷️🔒📦 等）：**超出两个工作台范围，本轮未做**；后续如需全站统一可单独处理。

### 优先级 P3 — 移动端打磨（第三轮已实施 ✅，见下 E 节）
13. ~~**窄屏间距**~~ ✅ 已完成
14. ~~**触控目标 ≥40px**~~ ✅ 已完成

---

### E. P2/P3 一致性与移动端（第三轮已实施 ✅）

| 项 | 改造前 | 改造后 |
|---|---|---|
| ⑧ 图标尺寸 token | 散用 `:size="14/15/16"` | `Icon.vue` 默认 16；新增 `.icon-xs/.sm/.md/.lg/.xl` 工具类（CSS 覆盖 svg 尺寸），统一 token 控制 |
| ⑨ 无障碍 | SVG 无语义、部分按钮无可读标签 | `Icon` 增 `label` prop（有 label → `role=img`+`aria-label`，否则 `aria-hidden`）；两个工作台菜单项/折叠按钮/复制按钮补 `aria-label`；顺手清掉工作台内残留 emoji（👁️/🙈/🔑/👤 → eye/eye-off/copy SVG），新增 `eye`/`eye-off` 图标 |
| ⑩ 骨架屏 | 首页数据来自 IDB，首次加载空白闪跳 | 业务 store 增 `loaded` 标志；`BusinessHome` 加载中显示主指标 5 卡 + 低库存/排行占位骨架（shimmer 微动效，暗色适配，`prefers-reduced-motion` 关闭） |
| ⑪ 键盘可达性 | 仅 Alt+K / Ctrl+Alt+1..9 / Esc | 扩展 `useWorkbenchShortcuts`：`g` 在个人/销售工作台间切换、`[`/`]` 在当前工作台模块间循环（跳过被关闭菜单项）；两个视图均挂载；输入框内不误触 |
| ⑬ 窄屏间距 | 业务台首页 2 列时与卡片间距偏松 | 两个工作台移动端内容区内边距收窄至 12px；业务首页卡片间距收紧至 12px |
| ⑭ 触控目标 | 移动端部分图标按钮偏小 | 两个工作台侧栏菜单项/头部按钮 `min 40×40px`；密码面板行内图标按钮移动端 `min 40×40px`、输入框内眼睛按钮 `32×32px`（受输入高度限制） |

> 实施文件：`src/components/Icon.vue`（⑧⑨ + eye/eye-off）、`src/style.css`（⑧ 工具类）、`src/stores/workbenchBusiness.ts`（⑩ `loaded`）、`src/components/business/BusinessHome.vue`（⑩⑬）、`src/composables/useWorkbenchShortcuts.ts`（⑪）、`src/views/WorkbenchView.vue` + `src/views/BusinessView.vue`（⑨⑪⑬⑭）、`src/components/workbench/WorkbenchPassword.vue`（⑨⑭）、`src/components/workbench/WorkbenchLedger.vue`（⑨）。

---

## 三、构建校验
- 类型检查：`vue-tsc -b` 通过（EXIT=0，0 error TS）。
- 打包：`vite build` 通过（371 模块转译、所有 chunk 正常产出；`INEFFECTIVE_DYNAMIC_IMPORT` 与超大 chunk 为既有警告，与本次改动无关）。
- 注：本沙箱 `vue-tsc` 偶发 OOM（JS 堆 / Rust 渲染阶段），属环境内存压力，非代码问题；已用「直接 `vite build` 到临时目录」+「单独 `vue-tsc -b` 捕获真实退出码」两次验证通过。
- 校验通过后再合并到主分支（如走 PR）。

## 四、改动文件清单

### 轮次 1 — 美化（emoji→SVG + 侧栏折叠 + 问候头）
- `src/components/Icon.vue` — 新增 19 个业务/面板 SVG 图标
- `src/views/BusinessView.vue` — 侧栏 SVG + 折叠 + 设置/云同步按钮 SVG
- `src/views/WorkbenchView.vue` — 设置/云同步按钮 SVG
- `src/views/HomeView.vue` — 云同步标签去 emoji（全局一致）
- `src/components/business/BusinessHome.vue` — 问候头 + 统计卡/标题 SVG
- `src/components/business/BusinessExpenses.vue` / `BusinessInventory.vue` / `BusinessProducts.vue` / `BusinessCategoryManager.vue` — 子页 emoji → SVG
- `src/components/workbench/WorkbenchLedger.vue` / `WorkbenchWeight.vue` / `WorkbenchPomodoro.vue` / `WorkbenchPassword.vue` / `WorkbenchExercise.vue` / `WorkbenchSleep.vue` / `WorkbenchHome.vue` — 面板统计图标 emoji → SVG
- `src/stores/settings.ts` — 新增 `businessSidebarCollapsed` 折叠态字段

### 轮次 2 — P0 体验闭环
- `src/components/business/BusinessHome.vue` — 统计卡/低库存卡点击下钻（P0-1）
- `src/views/BusinessView.vue` — `selectSection` 持久化当前模块（P0-2）
- `src/stores/settings.ts` — 新增 `businessActiveSection` 字段（类型/ref/序列化/反序列化/重置/setter + `BUSINESS_SECTION_KEYS` 白名单）
- `src/types/index.ts` — `AppSettingsData` 补 `businessActiveSection?`
- `src/components/workbench/WorkbenchPassword.vue` — 空闲自动锁定（P0-3）

### 轮次 3 — P2/P3 一致性与移动端
- `src/components/Icon.vue` — 新增 `label` 无障碍 prop + `eye`/`eye-off` 图标；`class` 透传配合尺寸 token
- `src/style.css` — 新增 `.icon-xs/.sm/.md/.lg/.xl` 图标尺寸工具类（⑧）
- `src/stores/workbenchBusiness.ts` — 新增 `loaded` 首屏加载标志（⑩）
- `src/components/business/BusinessHome.vue` — 骨架屏 + 窄屏间距（⑩⑬）
- `src/composables/useWorkbenchShortcuts.ts` — 扩展 `g` 切换工作台、`[`/`]` 切模块（⑪）
- `src/views/WorkbenchView.vue` — 菜单项/折叠按钮 `aria-label` + 挂载快捷键 + 移动端 40px/间距（⑨⑪⑬⑭）
- `src/views/BusinessView.vue` — 同上 + 挂载快捷键（⑨⑪⑬⑭）
- `src/components/workbench/WorkbenchPassword.vue` — 复制/显示密码图标 SVG + `aria-label` + 移动端触控目标（⑨⑭）
- `src/components/workbench/WorkbenchLedger.vue` — 金额显隐 eye/eye-off SVG（⑨）
