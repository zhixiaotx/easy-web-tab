# 2026-08-28

## P0 界面设计整改（导航 + 工作台互链 + 对比度）
实施此前评审文档中的 P0 三项，全部未提交（保持 review 状态）：

1. **P0-① 导航 emoji→SVG（HomeView）**：`Icon.vue` 新增 `help`/`toolbox` 图标；首页右上「设置/帮助」、左上「工作台/销售台」四个 emoji（⚙️❓🧰💰）替换为 `<Icon>` SVG；设置/帮助按钮补 `aria-label`，`.btn-help` 改为 inline-flex 对齐图标。`store` 图标复用自业务图标集。
2. **P0-② 两工作台互链**：`WorkbenchView` 头部加「销售记账工作台」按钮（`store` 图标 → /business）；`BusinessView` 头部加「个人工作台」按钮（`toolbox` 图标 → /workbench）。两者均按 `businessPageVisible`/`workbenchPageVisible` 开关显隐（与首页导航同源）。
3. **P0-③ 弱文本对比度**：`src/style.css` 中 `--color-text-muted` 由 `#94a3b8`（≈2.4:1 不达标）改为 `#64748b`（达 WCAG AA 4.5:1）。

验证：`vue-tsc -b` 0 错误；`vite build` 成功（构建到 Temp 临时目录规避沙箱 rm 拦截，未污染仓库）。

边界：Admin 侧（DisplayView/CategoryManager/AppSettingsDialog 等）残留 emoji 不在本次范围，留待后续（原评审 P1-⑫）。

## P1 界面设计整改（Token 统一 + App Bar + 字号阶梯）
延续 P0，实施评审 P1 三项，仍保持未提交（review 状态）：

4. **P1-④ 统一设计 Token 为单套语义变量**：对 `src/**/*.{vue,css}`（排除 `AGENTS.md`）执行 16 条 sed 重命名，将暗色并行命名（`--bg-*`/`--text-*`/`--border-*`/`--accent-*`/`--success-*`/`--warning-*`/`--error-*`/`--hover-bg`/`--input-bg`/`--shadow-color`）收敛为与亮色一致的 `--color-*` 语义变量。`src/style.css` 亮色 `:root` 补 `--color-bg-input`/`--color-shadow`；`src/styles/dark.css` 暗色 `:root.dark` 同步改为 `--color-*`（删除 sed 产生的重复 `--color-bg-card` 行）。跨体系兜底写法 `var(--x, fallback)` 保留。
5. **P1-⑤ 导航重组为统一 App Bar（HomeView）**：原分散的 `.top-left-toolbar` + `.top-right-toolbar` 两个 fixed 浮动栏合并为单一 `.app-bar`（fixed 满宽、space-between：`app-bar-left`=工作台/销售台入口，`app-bar-right`=主题/同步/设置/帮助/前台）。浮动白卡按钮改为玻璃拟态 ghost 按钮（透明底 + hover 高亮）；`.container` padding-top 由 70px→84px 让位；暗色用 `rgba(31,41,55,.82)` 背板；窄屏（≤768px）隐藏入口文字仅留图标、缩进内边距，避免左右两组互相挤压或与搜索框冲突。
6. **P1-⑥ 字号阶梯 + tabular-nums**：`src/style.css` 新增 `--font-size-2xl/3xl/4xl` 与 `--font-weight-normal/medium/semibold/bold` token，及 `.tabular-nums` 工具类（`font-variant-numeric: tabular-nums`）。`WorkbenchView` 与 `BusinessView` 的 h1 标题由 `18px/600` 改为 `var(--font-size-2xl,20px)/var(--font-weight-semibold,600)`；两视图 `.wb-content`/`.bs-content` 加 `font-variant-numeric: tabular-nums` 实现金额列等宽对齐。

验证：`vue-tsc -b` EXIT=0；`vite build` 成功（输出 Temp 临时目录，EXIT=0）。遗留告警（INEFFECTIVE_DYNAMIC_IMPORT / chunk>500kB）为历史既有，与本次无关。

下一步可选：① git 提交本次 P0+P1 改动；② 走查 Admin 侧 emoji（原评审 P1-⑫）；③ 其余评审项（P2/P3）按需推进。

## 走查 Admin 侧 emoji（全仓扫描）
全量脚本扫描 `src/**/*.{vue,ts,js}`：共 **411 处 emoji / 42 文件**。按可处理性分三类：

- **类别 1（装饰标题/区头，易 1:1 替换）**：AppSettingsDialog(⚙️设置/📥📤导入导出×多/🔗检测断链/🔍/🖼/📦/🎨/❗)、BackgroundManager(🎨背景管理/🚫无/🎨纯色/🌈渐变/🖼️图片/✏️修改渐变/📁上传)、BackupManager(📦数据备份管理/✅❌/📥导出/🔄刷新/🗑️清空/📋备份历史/💡说明)、IconManager(🎨图标管理/📥导入/📤导出/📁选择/🔍搜索)、DisplayView(❓帮助)、HelpModal(💡使用帮助/🎯功能介绍/📄页面模式/💾数据存储/📦下载)、CloudSyncConflictModal(⚠️冲突/🔀合并/☁️云端/💻本地/✖️取消)。
- **类别 2（功能性按钮 glyph：✕关闭/✓保存/✏️编辑/🗑️删除/⚠️警告/🔒锁定）**：跨 AppSettingsDialog/BackupManager/CategoryManager/CloudSyncConflictModal/CountdownModal/IconManager/SearchEngineManager/SiteModal/SpotlightOverlay/Business*/Workbench*/Toast 等全站通用控件。需先为 Icon.vue 补 close/check/pencil/trash/lock/warning 等 SVG。
- **类别 3（emoji 即数据，需设计决策，不可简单删）**：CategoryManager `defaultIcons` 数组(~100+)、useWeather.ts 天气码→emoji 映射、stores/categories.ts·types/index.ts·useBackup.ts 分类图标 emoji、ThemeToggle 🌙☀️、HelpModal 的 icon 字段、CalendarAnchorCard 🎂。这些属"分类图标选择器/天气/主题"功能，直接替换需建整套 SVG 图标体系+迁移路径。

当前 Icon.vue 缺口（执行 1/2 前须补）：link/image/download/upload/refresh/folder/info/package/palette/sun/moon/question/merge + close/check/pencil/trash。已具备：cog/help/search/cloud/lock/alert/tag/copy。

建议三阶段：A=类别1装饰标题；B=A+类别2通用控件 glyph（需补 SVG 库）；C=类别3 emoji 数据体系（单独立项）。待用户确认范围后实施。
