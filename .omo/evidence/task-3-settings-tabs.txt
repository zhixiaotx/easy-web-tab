# Task 3 Evidence — settings.ts: workbench dialog ids + IndexedDB persistence

Date: 2026-08-10
Files modified: src/stores/settings.ts ONLY (committed)
Server: pre-existing http://localhost:16718 (reused, not started by this task)
Tool: Playwright (browser_run_code_unsafe) via MCP

## Build gate
- `npm run build` exit 0 (vue-tsc strict + vite). No leftover 'countdown' DialogId usage anywhere.
- DialogId union no longer contains bare 'countdown' (only wb-countdown* ids); all 8 wb ids present.
- Exports added: NAV_DIALOG_IDS (6 ids, notes excluded), WB_DIALOG_IDS (9 ids).
- persist(): `idbPut('settings', toRaw({ dialogSizes: toRaw(dialogSizes.value), ... }))` fire-and-forget.
- initSettings() async: `idbGet<AppSettingsData>('settings')` → localStorage one-time non-destructive migration → applySettings.
- resetDefaults(): `void idbClear('settings')` + `localStorage.removeItem(SETTINGS_STORAGE_KEY)`.

## HAPPY QA (localStorage snapshot → one-time migration to IDB)
Setup: localStorage `user-app-settings` = {dialogSizes:{site:{width:900,height:80}},buttonOpacity:0.8,bgOpacity:0.9};
IndexedDB 'settings' store cleared. Reload page.

Result (page.evaluate):
- `--dlg-w-site` CSS var  == "900px"  (PASS — snapshot applied)
- IndexedDB 'settings' store now contains dialogSizes.site = {width:900,height:80}  (PASS — migration wrote IDB)
- localStorage key `user-app-settings` still present  (PASS — non-destructive, snapshot kept)

## FAILURE QA (IDB wins over stale localStorage snapshot)
Setup: IndexedDB 'settings' preset = {dialogSizes:{site:{width:700,height:70}},buttonOpacity:1,bgOpacity:1};
localStorage snapshot says site=900. Reload page.

Result (page.evaluate):
- `--dlg-w-site` CSS var == "700px"  (PASS — IDB priority, localStorage snapshot does NOT override)
- `--dlg-h-site` CSS var == "70vh"

## Console
- Errors: 0, Warnings: 0 (no DataCloneError; per-item toRaw(dialogSizes.value) confirmed working)

## Notes
- AppSettingsDialog.vue derives ids via Object.keys(DIALOG_DEFAULTS) — no hardcoded countdown reference; build confirms no breakage.
- No other files modified. No dev server started; pre-existing 16718 server left running.
